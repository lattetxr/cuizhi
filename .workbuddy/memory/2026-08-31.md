# 2026-08-31 工作日志

## 考研示例（首页示例三）内置认知地图

- 新增 `server/lib/kaoyanDataset.js`：内置知乎专栏文章《如果想考研，请听老学长一席话》（zhuanlan.zhihu.com/p/54779494）全文，以及「考研备考四步认知地图」KAOYAN_MAP（本质叩问→风险校准→目标锚定→行动闭环，含 5 个核心概念、4 条误区纠正、4 个应用场景）和 KAOYAN_COACH_OPENING。模式参照 `existenceDataset.js`。
- `server.mjs` /api/alchemy：URL 含 `54779494` 或粘贴正文含文章标题时走内置数据（`KAOYAN_MATCH`/`KAOYAN_KEYWORD`），用 KAOYAN_MAP 覆盖 pkg.mapData/concepts/path/scenarios，强制 `contentType='exam'`（前端以认知地图为主 tab），coachOpening 用内置开场白。
- 验证：URL 与粘贴正文两个入口均返回 4 步 learning_path、contentType=exam；npm test 17/18 通过，唯一失败为 anki.test.mjs 的 node:sqlite 兼容问题（`db.serialize is not a function`），与本次改动无关，属历史遗留。

## 首页/功能页 UI 优化

- `app.js` renderPackage：`state.tabName = getTypeConfig().primary`，进入学习包默认展示推荐功能 tab（exam→认知地图）；`visitedMapNodes` 初始化为 `Set([0])`。
- `styles.css`：`.path-node.current .path-index` 背景改 #0084ff（当前浏览节点蓝色）；`.map-runner` 40→64px；`.lks-greet` 96→144px；`.example-card` 改 flex column + `.example-pain { margin-top:auto }` 修复三卡片内容错位。
- `index.html` 示例三副标题改为「基于真实帖子·按关键阶段生成学习路径」。
- 首页示例卡片点击只是填充输入框（fillExample），需再点「开始炼金」才提交——自动化验证时容易踩坑。
- 验证方式：CDP 无头 Chrome 截图（参照 scripts/verify-browser.mjs 的 CdpClient），确认 map tab 默认激活、节点 1 蓝色、点击后节点变蓝、卡片几何坐标完全一致。

## 版面字体协调 + 看山对练加入刘看山 IP

- 认知地图详情字号上调：`.path-detail-card h4` 16→19px、`.path-detail-row span` 14→15.5px（行高 1.8）、strong 13→14px、`.path-label` 12→13px、`.path-source` 13→14px。
- 看山对练选择观点页新增 `.coach-hero` 头部（渐变浅蓝底 + playful.gif 76px + 标题/副文案 + 滑动提示），替换原 debate-select-head；对练中刘看山头像 32→40px，用户头像 28→32px，ai-name/ai-source/debate-summary/debate-start/debate-progress/scroll-hint 等 12–14px 小字统一上调 1px。
- 注意：styles.css 中 `.debate-options`/`.debate-option` 存在两处重复定义（约 2890 与 3178 行，后者覆盖前者为横向滚动卡片），后续重构可合并。
- 欢迎页跳过方式：`sessionStorage.setItem('cuizhi_welcome_seen','1')` + 移除 `#welcomeView`。

